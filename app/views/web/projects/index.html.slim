.project-gallery
  .gallery
    .gallery-inner
      - @projects.each do |project|
        .item
          .title= link_to project.title, project_path(project)
          = link_to project_path(project) do
            img src="#{project.preview.url}" width=188 height=250
    ul.gallery-paginator